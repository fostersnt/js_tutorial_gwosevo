/*The .reverse() method in JavaScript is an array method that reverses 
the elements of an array in place. This means that the original array 
is modified and its elements are rearranged in the opposite order.
*/

const numbers = [1, 2, 3, 4, 5];

const reversedNumbers = numbers.reverse();
console.log(reversedNumbers); 