/*
The .concat() method in JavaScript is used to merge two or more arrays. 
It creates a new array that contains the elements from the original arrays without modifying them. 
*/

const array1 = [1, 2, 3];
const array2 = [4, 5, 6];

const newArray = array1.concat(array2);
console.log(newArray); 