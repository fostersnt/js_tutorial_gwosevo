/*
The .sort() method in JavaScript is an array method that sorts the elements of 
an array in place and returns the sorted array.
*/

const fruits = ['banana', 'apple', 'orange', 'mango'];

fruits.sort();
console.log(fruits); 
//so this will sort them in alphabetical order