// The .keys() function returns a new Array Iterator object containing the keys (indices) of the array.

// Example:
const numbers = [10, 20, 30];
const keys = numbers.keys();

for (let key of keys) {
  console.log(key); // Output: 0, 1, 2
}