// The .forEach() function executes a provided function once for each array element.
// It does not return a new array or modify the original one.

// Example:
const numbers = [1, 2, 3];
numbers.forEach((num) => console.log(num * 2));