// The .findIndex() function returns the index of the first element that satisfies a specified condition.
// If no element satisfies the condition, it returns -1.

// Example:
const numbers = [4, 7, 10, 15];
const index = numbers.findIndex((num) => num > 10);

console.log(index); // Output: 3