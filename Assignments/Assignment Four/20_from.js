/*
The Array.from() method in JavaScript is a static method used to
 create a new array instance from an array-like objecty
*/

const str = 'hello';

const charArray = Array.from(str);
console.log(charArray);