// The .reverse() function reverses the order of elements in an array in place.
// It modifies the original array.

// Example:
const numbers = [1, 2, 3];
numbers.reverse();

console.log(numbers); // Output: [3, 2, 1]